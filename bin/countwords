#!/bin/bash

LINES=$(cat "$1" | wc -l)

if [[ -z "$START" ]]; then START=0; fi
if [[ -z "$END" ]]; then END=$LINES; fi

echo "DEBUG: LINES=$LINES START=$START END=$END"
echo -n "Words: "
tail -n $(($LINES - $START)) "$1" | head -n $(($END - $START)) - | tr -d '\n' | tr ' ' '\n' | wc -l

